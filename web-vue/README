# web-vue 前端说明

当前前端基于 `Vue 3 + TypeScript + Vite + Element Plus`，主职责是：

- 登录与基础页面路由
- 环境监测展示
- 监控页播放视觉流并控制推理/录像
- 检测记录查询与处理
- 模型上传与切换

前端默认把 Spring Boot 作为主 API 入口，基地址优先取：

- `VITE_API_BASE_URL`
- `VITE_API_URL`
- 默认值 `/api`

## 启动

```bash
cd /home/orangepi/Desktop/web/bishebeifen-master/web-vue
npm install
npm run dev -- --host 0.0.0.0 --port 3000
```

构建：

```bash
npm run build
```

## Vite 代理

`vite.config.ts` 当前代理如下：

| 前缀 | 转发目标 | 说明 |
|---|---|---|
| `/api` | `http://localhost:8080` | 当前主后端 |
| `/algo` | `http://localhost:5000` | 历史算法代理，现主链路未使用 |
| `/file` | `http://localhost:5001` | 历史文件代理，现主链路通常直接走 `/api/file` |

## 当前有效路由

| 路由 | 页面 | 说明 |
|---|---|---|
| `/login` | 登录页 | 无需登录 |
| `/register` | 注册页 | 无需登录 |
| `/dashboard` | 首页 | 环境监测、声音监测、视觉统计 |
| `/profile` | 个人中心 | 头像上传、个人资料修改 |
| `/monitor` | 监控管理 | 摄像头列表、推理开关、录像、模型切换 |
| `/detection/record` | 检测记录管理 | 查询、处理、删除安全记录 |
| `/model/upload` | 上传模型 | 上传 `.rknn + .txt` 并切换模型 |
| `/admin` | 管理控制台 | 管理员首页 |
| `/admin/users` | 用户管理 | 依赖遗留用户管理接口，当前后端未完全实现 |

补充：

- 当前 `/camera` 路由会直接重定向到 `/dashboard`
- `SoundMonitoring.vue`、`camera/index.vue`、`user/index.vue` 组件仍在仓库里，但不属于当前主路由链路

## 环境变量

| 变量 | 默认值 | 用途 |
|---|---|---|
| `VITE_API_BASE_URL` | `/api` | 主后端地址 |
| `VITE_API_URL` | `/api` | 主后端地址备用 |
| `VITE_FILE_URL` | 跟随主后端 | 文件接口地址，可单独覆盖 |
| `VITE_STREAM_PROTOCOL` | `webrtc` | 流播放协议，支持 `webrtc` 或 `hls` |
| `VITE_WEBRTC_BASE_URL` | `http://<当前页面主机>:8889` | WHEP 基地址，通常无需手写 |
| `VITE_STREAM_HOST` | 空 | 强制覆盖流媒体主机 |
| `VITE_ALGO_URL` | 空 | `AlgoHealthCheck` 组件的独立算法地址 |
| `VITE_ALGO_WS_URL` | 空 | 旧 WebSocket 检测链路，未配置时自动禁用 |

## 播放链路

`src/components/VideoPlayer/index.vue` 当前逻辑：

- 默认协议是 `webrtc`
- 若 `VITE_STREAM_PROTOCOL=hls`，则改为 HLS
- 前端会把后端返回的 `rtsp://<host>:8554/camX` 自动转换成
  - HLS：`http://<host>:8888/camX/index.m3u8`
  - WHEP：`http://<host>:8889/camX/whep`

正常情况下不需要再手写 `VITE_WEBRTC_BASE_URL`。只有 WebRTC 部署在另一台机器时，才建议单独指定。

## 请求封装

### `src/api/request.ts`

- 主后端请求封装
- 自动从 `localStorage.userInfo.token` 读取 JWT
- 默认把统一响应体里的 `data` 解包后返回

### `src/api/file_request.ts`

- 文件上传/读取/删除封装
- 仍然从 `localStorage.userInfo.token` 附带认证头
- 当前常用 bucket
  - `avatars`
  - `models`
  - `detection`
  - `process`

## 页面与 HTTP 对照

### 首页 `/dashboard`

主要调用：

- `GET /api/sensor/latest`
- `GET /api/sensor/threshold`
- `PUT /api/sensor/threshold`
- `PUT /api/sensor/monitoring?enabled=true|false`
- `POST /api/sensor/refresh`
- `GET /api/sensor/monitoring`
- `POST /api/sound/start`
- `POST /api/sound/stop`
- `GET /api/sound/status`
- `POST /api/sound/upload`
- `GET /api/rknn/detection/count?cam=0|1`

### 监控页 `/monitor`

主要调用：

- `GET /api/camera`
- `GET /api/detection/record/page`
- `GET /api/model/list`
- `POST /api/model/select?id=...`
- `GET /api/rknn/status`
- `POST /api/rknn/inference/on`
- `POST /api/rknn/inference/off`
- `POST /api/rknn/threshold/set`
- `POST /api/rknn/tracker/set?enabled=true|false`
- `POST /api/rknn/rtsp/camera/start`
- `GET /api/rknn/record/status`
- `POST /api/rknn/record/start?cameraId=1|2`
- `POST /api/rknn/record/stop?cameraId=1|2`
- `GET /api/rknn/record/files`
- `GET /api/rknn/record/file?name=...`

### 检测记录页 `/detection/record`

主要调用：

- `GET /api/detection/record/page`
- `GET /api/detection/record/{id}`
- `PUT /api/detection/record/process`
- `PUT /api/detection/record/{id}/process`
- `DELETE /api/detection/record/{id}`
- `DELETE /api/detection/record/batch`
- `DELETE /api/detection/record/clear-all`
- `GET /api/camera`
- `GET /api/sound/audio?path=...`

当前页面真实行为补充：

- 详情页会显示同一条安全记录的 `AI 融合分析`
- 前端读取的字段包括
  - `aiAnalysisStatus`
  - `aiAnalysisResult`
  - `aiAnalysisTime`
- 声音记录详情默认显示声音信息与音频播放器
- 视频记录详情默认显示检测图像
- 如果声音记录想显示图片，前提是后端该条记录本身带了 `imageUrl`

### 个人中心 `/profile`

主要调用：

- `PUT /api/user`
- `POST /api/file/upload/avatars`
- `GET /api/file/avatars/...`

### 上传模型 `/model/upload`

主要调用：

- `POST /api/file/upload/models`
- `GET /api/model/list`
- `GET /api/model/current`
- `POST /api/model/select?id=...`

## 当前代码里的遗留点

- `src/api/user.ts` 仍有 `/user/page`、`/user/{id}`、`/user/password`、`/user/{id}/reset-password` 等封装
- 当前 Spring Boot 实际只实现了
  - `POST /user/login`
  - `POST /user/register`
  - `POST /user/logout`
  - `PUT /user`
- 因此 `/admin/users` 页面如果继续使用这些遗留接口，联调前需要先补齐后端

## 推荐联调

```bash
curl http://127.0.0.1:8080/api/test/health
curl http://127.0.0.1:8080/api/rknn/status
curl http://127.0.0.1:8080/api/sensor/latest
```

如果监控页播放异常，优先检查：

- `http://127.0.0.1:8080/api/rknn/status`
- `http://127.0.0.1:8091/api/status`
- `ss -ltnup | grep -E '8554|8888|8889'`
