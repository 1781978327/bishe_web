# 环境监测数据读取系统说明文档

## 目录
1. [系统架构](#系统架构)
2. [硬件连接](#硬件连接)
3. [传感器读取程序](#传感器读取程序)
4. [后端服务](#后端服务)
5. [前端展示](#前端展示)
6. [编译与部署](#编译与部署)
7. [常见问题](#常见问题)

---

## 系统架构

```
┌─────────────────────────────────────────────────────────────────┐
│                          硬件层                                   │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐       │
│  │  DHT11   │  │   MQ-2   │  │  光照    │  │  ADS1115 │       │
│  │ 温湿度   │  │  烟雾    │  │  传感器  │  │   ADC    │       │
│  │ 传感器   │  │  传感器   │  │  GL5528  │  │  转换器  │       │
│  └────┬─────┘  └────┬─────┘  └────┬─────┘  └────┬─────┘       │
│       │             │             │             │              │
│       └─────────────┴─────────────┴─────────────┘              │
│                           │                                    │
│                    ┌──────┴──────┐                             │
│                    │ sensor_reader │                          │
│                    │  整合读取程序  │                            │
│                    └──────────────┘                             │
└──────────────────────────┬────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────────┐
│                          后端层 (Spring Boot)                    │
│  ┌──────────────────┐  ┌──────────────────┐                     │
│  │  SensorService   │──│  SensorData      │                     │
│  │  定时读取传感器   │  │  数据持久化      │                     │
│  └────────┬─────────┘  └──────────────────┘                     │
│           │                                                      │
│  ┌────────┴─────────┐                                          │
│  │   REST API 接口   │                                          │
│  │  /api/sensor/*    │                                          │
│  └────────┬─────────┘                                          │
└───────────┼────────────────────────────────────────────────────┘
            │
            ▼
┌─────────────────────────────────────────────────────────────────┐
│                          前端层 (Vue 3)                          │
│  ��──────────────────┐  ┌──────────────────┐                     │
│  │    Dashboard     │  │ RecordManagement │                     │
│  │  实时数据展示    │  │  记录详情查看    │                     │
│  └──────────────────┘  └──────────────────┘                     │
└─────────────────────────────────────────────────────────────────┘
```

---

## 硬件连接

### 硬件清单

| 传感器 | 接口类型 | 数量 | 说明 |
|--------|----------|------|------|
| DHT11 | GPIO (wPi=2) | 1 | 温度/湿度测量 |
| MQ-2 | ADC (AIN0) | 1 | 烟雾浓度检测 |
| 光照传感器 | ADC (AIN1) | 1 | GL5528光敏电阻 |
| ADS1115 | I2C | 1 | 16位ADC转换器 |

### 引脚连接

```
香橙派5B GPIO 引脚:
┌────────────────────────────┐
│  物理引脚    功能    wPi编号 │
├────────────────────────────┤
│    1      3.3V电源    -     │
│    3      GPIO/I2C   -     │
│    5      GPIO/I2C   -     │
│    6      GND        -     │
│    7      GPIO       2  ← DHT11 Data    │
│    9      GND        -     │
│   40      5V电源     -     │
└────────────────────────────┘

ADS1115 I2C 连接:
┌──────────────────────────────┐
│  ADS1115      香橙派5B       │
│  VDD    ────   3.3V          │
│  GND    ────   GND           │
│  SCL    ────   I2C1 SCL      │
│  SDA    ────   I2C1 SDA      │
│  A0     ────   MQ-2 传感器   │
│  A1     ────   光照传感器    │
└──────────────────────────────┘
```

---

## 传感器读取程序

### 程序结构

```bash
Hardware/
├── sensor_reader_http.cpp       # 服务入口（main + signal）
├── CMakeLists.txt               # CMake 构建配置
├── src/
│   ├── dht11_module.h/.cpp      # DHT11 温湿度读取（GPIO bit-bang）
│   ├── ads1115_module.h/.cpp    # ADS1115 I2C、MQ-2 烟雾、光照转换
│   └── sensor_http_server.h/.cpp # libmicrohttpd 服务、JSON、轮询线程
├── dht11/                       # DHT11 独立测试程序
├── ads1115/                     # ADS1115 独立测试程序
└── build/                       # CMake 构建输出目录
```

### sensor_reader.cpp 功能

1. **DHT11 读取**：温度、湿度
2. **ADS1115 读取**：MQ-2烟雾、GL5528光照
3. **JSON输出**：统一的数据格式

### 输出格式

```json
{
  "temperature": 28.5,
  "humidity": 65.0,
  "smoke": 45.0,
  "light": 320.0
}
```

### 编译方法

```bash
cd /home/orangepi/Desktop/web/bishebeifen-master/Hardware

# 使用 CMake 编译
mkdir -p build && cd build
cmake ..
make -j$(nproc)

# 运行（需要sudo权限访问GPIO和I2C）
cd .. && sudo ./build/sensor_reader_http
```

---

## 后端服务

### 技术栈

- **框架**: Spring Boot 3.2
- **数据库**: H2 (内存数据库，用于演示)
- **定时任务**: @Scheduled

### 核心类

```
com.example.demo/
├── service/
│   └── SensorService.java     # 传感器数据处理核心服务
├── entity/
│   └── SensorData.java       # 传感器数据实体
├── controller/
│   └── SensorController.java # REST API控制器
└── repository/
    └── SensorDataRepository.java # 数据访问层
```

### SensorService 核心逻辑

```java
// 1. 定时任务：每5秒读取一次传感器数据
@Scheduled(fixedRate = 5000)
public void scheduledSensorRead() {
    if (monitoringEnabled.get()) {
        readSensorDataFromHardware();
    }
}

// 2. 执行C++程序读取硬件数据
private String executeCommand(String command) {
    Process process = Runtime.getRuntime().exec(command);
    // 读取程序输出（JSON格式）
    BufferedReader reader = new BufferedReader(
        new InputStreamReader(process.getInputStream()));
    return reader.readLine();
}

// 3. 解析JSON并保存
private void parseSensorJson(String json, SensorData data) {
    // 解析温度、湿度、烟雾、光照
}

// 4. 报警检测
private void checkAlert(SensorData data) {
    // 检测是否超过阈值
}
```

### REST API 接口

| 接口 | 方法 | 说明 |
|------|------|------|
| `/api/sensor/latest` | GET | 获取最新传感器数据 |
| `/api/sensor/thresholds` | GET | 获取阈值设置 |
| `/api/sensor/thresholds` | PUT | 更新阈值 |
| `/api/sensor/monitoring/start` | POST | 开启监测 |
| `/api/sensor/monitoring/stop` | POST | 停止监测 |

### 阈值配置

```java
thresholds.put("temperature", 35.0f);  // 温度阈值
thresholds.put("humidity", 80.0f);     // 湿度阈值
thresholds.put("smoke", 100.0f);       // 烟雾阈值
thresholds.put("light", 500.0f);       // 光照阈值
```

---

## 前端展示

### 页面组件

| 页面 | 组件 | 功能 |
|------|------|------|
| Dashboard | 环境监测数据卡片 | 实时显示温湿度、烟雾、光照 |
| RecordManagement | 环境监测记录详情 | 查看历史环境数据 |

### 数据流

```
后端API → axios.get('/api/sensor/latest') → 前端状态 → 页面显示
```

### Dashboard.vue 核心代码

```javascript
// 1. 定义响应式数据
const sensorData = ref({
  temperature: '--',
  humidity: '--',
  smoke: '--',
  light: '--'
})

// 2. 定时获取数据（每1秒）
const refreshTimer = window.setInterval(() => {
  fetchLatestSensorData()
}, 1000)

// 3. 获取数据函数
const fetchLatestSensorData = async () => {
  const response = await axios.get(`${apiBaseUrl}/sensor/latest`)
  if (response.data.code === 200) {
    sensorData.value = {
      temperature: data.temperature.toFixed(1),
      humidity: data.humidity.toFixed(1),
      smoke: Math.round(data.smoke),
      light: Math.round(data.light)
    }
  }
}
```

### RecordManagement.vue 环境数据解析

```javascript
// 从 processContent 字段解析环境数据
// 格式: "温度: 28.4°C, 湿度: 67.3%, 烟雾: 68.8ppm, 光照: 314.0lux"

const envData = computed(() => {
  const content = currentRecord.value?.processContent || ''
  const data = {}
  
  const tempMatch = content.match(/温度:\s*([\d.]+)/)
  if (tempMatch) data.temperature = parseFloat(tempMatch[1])
  
  const humMatch = content.match(/湿度:\s*([\d.]+)/)
  if (humMatch) data.humidity = parseFloat(humMatch[1])
  
  // ... 烟雾、光照同理
  return data
})
```

---

## 编译与部署

### 1. 编译传感器读取程序

```bash
# 使用 CMake 编译
cd ~/Desktop/web/bishebeifen-master/Hardware
mkdir -p build && cd build
cmake ..
make -j$(nproc)
cd ..
# 二进制位于 build/sensor_reader_http
sudo ./build/sensor_reader_http
```

### 2. 启动后端服务

```bash
cd ~/Desktop/web/bishebeifen-master/web-springboot/demo3/demo

# 编译
../gradle-8.14.1/bin/gradle compileJava

# 启动
../gradle-8.14.1/bin/gradle bootRun

# 或者使用脚本
./start_as_root.sh
```

### 3. 启动前端服务

```bash
cd ~/Desktop/web/bishebeifen-master/web-vue

# 安装依赖
npm install

# 开发模式启动
npm run dev

# 生产构建
npm run build
```

---

## 常见问题

### 1. DHT11 读取不稳定

**原因**: DHT11需要精确的时序控制

**解决方案**:
- 增加重试机制（代码中已实现3次重试）
- 检查GPIO连接是否牢固
- 确保接线正确（物理7脚对应wPi编号2）

### 2. I2C 设备无法打开

**错误信息**: `I2C设备打开失败`

**解决方案**:
```bash
# 检查I2C设备
ls -la /dev/i2c-*

# 检查用户权限
groups $USER
# 应该包含 i2c 和 gpio 组

# 如需添加权限
sudo usermod -aG i2c,gpio $USER
```

### 3. 后端显示模拟数据

**检查步骤**:
1. 确认 `sensor_reader` 程序可正常运行
2. 检查后端日志是否显示"使用模拟传感器数据"
3. 确认传感器读取程序路径正确

```bash
# 测试传感器程序
sudo ./Hardware/sensor_reader

# 检查后端日志
tail -f demo/backend.log | grep 传感器
```

### 4. 前端显示 0.0 或 --

**检查步骤**:
1. 打开浏览器F12控制台
2. 检查网络请求是否成功
3. 查看API返回数据格式

```javascript
// 在Dashboard.vue中添加调试
console.log('传感器数据:', response.data.data)
```

---

## 文件路径汇总

| 文件 | 路径 |
|------|------|
| 传感器读取程序 | `~/Desktop/web/bishebeifen-master/Hardware/sensor_reader_http.cpp` |
| 后端服务主类 | `~/Desktop/web/bishebeifen-master/web-springboot/demo3/demo/src/main/java/com/example/demo/DemoApplication.java` |
| 传感器服务 | `~/Desktop/web/bishebeifen-master/web-springboot/demo3/demo/src/main/java/com/example/demo/service/SensorService.java` |
| 前端Dashboard | `~/Desktop/web/bishebeifen-master/web-vue/src/views/Dashboard.vue` |
| 记录管理 | `~/Desktop/web/bishebeifen-master/web-vue/src/views/detection/RecordManagement.vue` |
| Gradle | `~/Desktop/web/bishebeifen-master/web-springboot/gradle-8.14.1/` |

---

## 版本信息

- **文档版本**: 1.0
- **创建日期**: 2026-03-30
- **最后更新**: 2026-03-30